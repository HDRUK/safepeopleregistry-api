# frankenphp.yaml
# workers:
#   command: php
#   max_requests: 500 # recycle worker after N requests
#   memory_limit: 256M # restart worker if exceeds this
#   reload_on_change: true

# listen: ":8100"

# document_root: public

# worker: public/frankenphp-worker.php

# workers:
#   command: php
#   reload_on_change: true
#   max_requests: 500
#   memory_limit: 256M

workers:
  command: php artisan octane:frankenphp --host=0.0.0.0 --port=8100
  max_requests: 250
  memory_limit: 256M
  reload_on_change: false